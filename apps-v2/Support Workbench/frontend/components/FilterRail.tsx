// Kept for backwards compatibility. The filter UI now lives in FilterMenu
// (a toolbar button + popover) so the ticket table can use the full width.
export { FilterMenu, DEFAULT_FILTERS, activeFilterCount } from './FilterMenu'
export type { TicketFilters } from './FilterMenu'
